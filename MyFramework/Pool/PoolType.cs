namespace MyFramework.Pool
{
    // 完全复用您原始定义的PoolType
    public enum PoolType  
    {  
        UI,  
        Monster,  
        Enemy,  
        GameObject,  
        EnemySpawnPoint,  
        DropItem,  
        Asset,  
        Timer,  
        Bullet,  
        Effect,  
    }
}